 const arcanosMayores = [ "0","I","II","III","IV","V","VI","VII","VIII","IX","X","XI","XII","XIII","XIV","XV","XVI","XVII","XVIII","XIX","XX","XXI" ];const imagenesCartas ={"0":"00.png","I":"01.png","II":"02.png","III":"03.png","IV":"04.png","V":"05.png","VI":"06.png","VII":"07.png","VIII":"08.png","IX":"09.png","X":"10.png","XI":"11.png","XII":"12.png","XIII":"13.png","XIV":"14.png","XV":"15.png","XVI":"16.png","XVII":"17.png","XVIII":"18.png","XIX":"19.png","XX":"20.png","XXI":"21.png",};function sacarUnaCarta(){const indice = Math.floor(Math.random() * arcanosMayores.length);return arcanosMayores[indice];}function sacarTriada(){const copia = [...arcanosMayores];const triada = [];for (let i = 0;i < 3;i++){const index = Math.floor(Math.random() * copia.length);triada.push(copia.splice(index,1)[0]);}return triada;}const resultado = document.getElementById("resultado");let currentTarotLang = "de";const urlParams = new URLSearchParams(window.location.search);const selectedDeck = urlParams.get("deck");const defaultFolder = "tarot_caravaggio";const tarotFolders ={de:selectedDeck || defaultFolder,en:selectedDeck || defaultFolder,es:selectedDeck || defaultFolder,};let lastDrawType = null;let lastCards = null;function crearCartaHTML(nombreCarta){const nombreArchivo = imagenesCartas[nombreCarta] || "reverse.webp";const cartaId = `carta-${Math.random().toString(36).substr(2,9)}`;const folder = tarotFolders[currentTarotLang] || tarotFolders.de;console.log("Usando carpeta:",folder,"para carta:",nombreCarta);return ` <div class="tarot-card" id="${cartaId}"> <div class="card-inner"> <div class="card-side card-front"> <img src="${folder}/${nombreArchivo}" alt="${nombreCarta}"> </div> <div class="card-side card-back"> <img src="${folder}/reverso.png" alt="Reverso"> </div> </div> </div> `;}function renderUnaCarta(carta){resultado.innerHTML = crearCartaHTML(carta);agregarVolteo();}function renderTriada(cartas){resultado.innerHTML = cartas.map(c => crearCartaHTML(c)).join("");agregarVolteo();}function agregarVolteo(){const cartas = document.querySelectorAll(".tarot-card");cartas.forEach(carta =>{const flip = () =>{carta.classList.add("flipped");carta.removeEventListener("click",flip);};carta.addEventListener("click",flip);});}const unaCartaBtn = document.getElementById("una-carta");if (unaCartaBtn){unaCartaBtn.addEventListener("click",() =>{const carta = sacarUnaCarta();lastDrawType = "one";lastCards = carta;renderUnaCarta(carta);});}const triadaBtn = document.getElementById("triada");if (triadaBtn){triadaBtn.addEventListener("click",() =>{const cartas = sacarTriada();lastDrawType = "triad";lastCards = cartas;renderTriada(cartas);});}document.addEventListener("click",(e) =>{const btn = e.target.closest(".lang-button");if (!btn) return;const lang = btn.dataset.lang;if (lang === "de" || lang === "en"){currentTarotLang = lang;console.log("Idioma del tarot ahora:",currentTarotLang);if (lastDrawType === "one" && lastCards){renderUnaCarta(lastCards);}else if (lastDrawType === "triad" && Array.isArray(lastCards)){renderTriada(lastCards);}else{resultado.innerHTML = "";}}});function getCurrentLang(){if (typeof window !== "undefined" && window.currentLang){return window.currentLang;}if (typeof currentLang !== "undefined" && currentLang){return currentLang;}return "de";}function getOracleText(key){const lang = getCurrentLang();return (languagesContent && languagesContent[lang] && languagesContent[lang][key]) || '';}function getCardsArray(){if (lastDrawType === "one" && lastCards){return [lastCards];}if (lastDrawType === "triad" && Array.isArray(lastCards)){return lastCards;}return [];}async function preguntarAlOraculo(){const questionEl = document.getElementById("tarot-question");const answerEl = document.getElementById("oracle-answer");if (!questionEl || !answerEl) return;const question = questionEl.value.trim();const cardsArray = getCardsArray();if (!cardsArray.length){answerEl.textContent = getOracleText('oraculo_primero_tirada');return;}if (!question){answerEl.textContent = getOracleText('oraculo_escribe_pregunta');return;}const cards = cardsArray.map((name) => ({name,position:"upright",}));answerEl.textContent = getOracleText('oraculo_consultando');try{const API_URL = "https:const response = await fetch(API_URL,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({question,cards,drawType:lastDrawType,language:getCurrentLang(),}),});const data = await response.json();if (!response.ok){console.error("Error desde /api/tarot:",data);answerEl.textContent = getOracleText('oraculo_error_servidor') + (data.error || getOracleText('oraculo_problema')) + (data.details ? getOracleText('oraculo_detalles') + data.details:"");return;}if (data.answer){answerEl.textContent = data.answer;}else{console.error("Respuesta inesperada desde /api/tarot:",data);answerEl.textContent = getOracleText('oraculo_silencio');}}catch (err){console.error("Error en fetch /api/tarot:",err);answerEl.textContent = getOracleText('oraculo_error_hablar');}}const askOracleBtn = document.getElementById("ask-oracle-btn");if (askOracleBtn){askOracleBtn.addEventListener("click",preguntarAlOraculo);}const tarotQuestionTextarea = document.getElementById("tarot-question");if (tarotQuestionTextarea){if (tarotQuestionTextarea.value.trim() && !tarotQuestionTextarea.placeholder){tarotQuestionTextarea.placeholder = tarotQuestionTextarea.value;tarotQuestionTextarea.value = "";}tarotQuestionTextarea.addEventListener("focus",function (){if (this.value === this.placeholder){this.value = "";}});}